[defaults]
become = true
become_method = sudo
default_become_user = root 
ansible_gather_timeout = 15
inventory = $ENV('HOME')/.ansible/hosts
interpreter_python = auto_legacy_silent

[environment]
DEBIAN_FRONTEND = noninteractive
